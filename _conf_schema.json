{
    "tag_name": {
        "description": "XML tag name",
        "hint": "The tag name that wraps the injected recursion log. Any characters except < > and line breaks are allowed.",
        "type": "string",
        "default": "Recursion-Log"
    },
    "header_text": {
        "description": "Header text before sections",
        "hint": "Explanatory text displayed inside the tag, before the first section. Supports \\n for line breaks.",
        "type": "text",
        "default": ""
    },
    "inject_position": {
        "description": "Injection position",
        "hint": "Where to inject the recursion log in the LLM request. user_message_before: before the user's message; user_message_after: after the user's message.",
        "type": "string",
        "default": "user_message_before",
        "options": [
            "user_message_before",
            "user_message_after"
        ]
    },
    "date_format": {
        "description": "Date display format",
        "hint": "How timestamps are displayed in the injected log. MM-DD for month-day, YYYY-MM-DD for full date.",
        "type": "string",
        "default": "MM-DD",
        "options": [
            "MM-DD",
            "YYYY-MM-DD"
        ]
    },
    "inner_footer_text": {
        "description": "Inner footer text (inside tag)",
        "hint": "Text displayed inside the tag, after the last section and before the closing tag. Supports \\n for line breaks.",
        "type": "text",
        "default": ""
    },
    "footer_text": {
        "description": "Footer text (outside tag)",
        "hint": "Text displayed after the closing tag. Placed directly before Felis Abyssalis's live message. Supports \\n for line breaks.",
        "type": "text",
        "default": "Below is Felis Abyssalis's live message. Execute from here."
    },
    "initial_context_count": {
        "description": "Initial context count",
        "hint": "Number of context entries already present when a new window opens (from preset conversations). Set to 0 if no preset; set to 2 if one user+assistant preset pair exists. Injection fires when actual context count <= this value.",
        "type": "int",
        "default": 2
    }
}
